// Google Analytics temporarily disabled for build recovery
// TODO: Re-enable by installing react-ga4 and restoring this file

export const initGA = () => {
  // No-op
};

export const trackPageView = (_path: string, _title?: string) => {
  // No-op
};
