"use client";

import React from "react";

/**
 * Client-only ad integration shell.
 *
 * This is intentionally NOT injecting any external scripts from SSR root.
 * Future Monetag integration can be implemented here safely.
 */
export function AdsClient() {
  // Placeholder: render reserved ad containers / loading UI only.
  return null;
}

